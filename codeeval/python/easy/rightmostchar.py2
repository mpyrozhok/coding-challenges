# -*- coding: utf-8 -*-
import sys
import string

def main():
    with open(sys.argv[1]) as f:
        for line in f:
            a, b = line.strip().split(',')
            print string.find(a, b)


if __name__ == "__main__":
    main()
